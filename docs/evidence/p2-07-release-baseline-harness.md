# Evidence Record

Feature/Gate: P2-07 release baseline harness

Date: 2026-05-24 Asia/Shanghai

Commit: included in this change; final hash recorded after commit

Environment: Windows local development machine. This is not the Linux native baseline host.

Command:

```text
cd backend && go test ./...
pnpm exec node tests/load/validate-k6-suite.mjs
cd backend && go run ./cmd/p1loadseed
pnpm run build
node tests/load/run-p2-linux-baseline.mjs --final
```

Raw Output Path:

- Final Linux run will write `docs/perf/raw/p2-07/`.
- This Windows validation records command summaries only and does not create capacity evidence.

## Setup

- Added `tests/load/multi-room-isolation.js`.
- Extended `tests/load/lib/live-auction.js` with room/auction-specific helpers.
- Extended `backend/cmd/p1loadseed` to seed hot and cold active rooms for baseline isolation tests.
- Added `tests/load/run-p2-linux-baseline.mjs` with smoke/final modes.
- Updated k6 suite validation to require the multi-room workload and final-run guardrails.

## Expected Invariant

- Existing k6 scripts still use `k6/websockets`, local imports, thresholds, and p99/p999 summary stats.
- P2-07 final mode cannot run on Windows/WSL-like local development by accident.
- Final mode must capture three raw runs for each workload before any performance claim is considered.

## Result

PASS for harness readiness. Final Linux baseline remains pending by design.

## Observed Data

- `cd backend && go test ./...` passed.
- `pnpm exec node tests/load/validate-k6-suite.mjs` passed.
- `cd backend && go run ./cmd/p1loadseed` passed and seeds `room_main/auc_live` plus `room_side/auc_side`.
- `pnpm run build` passed; PC bundle still emits the existing Vite chunk-size warning.
- `node tests/load/run-p2-linux-baseline.mjs --final` refused this Windows environment with: `P2-07 final baseline requires Linux native`.

## Known Limits

- No Linux baseline numbers are produced in this change, and no capacity claim is allowed from this evidence.
- The final report is generated by the runner but must still be reviewed for bottlenecks and system metrics before any claim is allowed.

## Next Action

Execute `node tests/load/run-p2-linux-baseline.mjs --final` on the Linux baseline host and review the generated raw outputs/report.
