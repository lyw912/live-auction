# 统一"快引擎 + 富功能"——行业架构调研与改造方案

> 2026-06-07 · 回应"双引擎功能不对等（fat-finger / proxy-max-bid 在 redis_ledger 下被 fail-closed 暂停）"的追问。
> 结论先行：**这是实现方式造成的伪两难，不是架构定律。** 真实工业界（eBay、交易所撮合、大厂系统设计参考）都在**同一条快路径上同时支持**原子决策 + 代理出价 + 防狙击 + 精确一次结算。本项目其实已经拥有 90% 的零件，缺的是把"复杂规则 = 退回 PG = 暂停"的过保守判定，换成行业标准的"复杂规则 = 经同一条快路径回灌的另一种出价来源"。

---

## 1. 本项目为什么会撞墙（根因复盘）

`engine.go:1066-1116 loadSnapshot` 把规则分类成：

```sql
CASE WHEN ar.fat_finger_threshold_cents IS NOT NULL THEN 'fat_finger_confirm'
     WHEN EXISTS(... max_bid_intents ACTIVE)        THEN 'active_max_bid_intent'
     ELSE '' END
```

只要 `RequiresPostgres != ''` → `e.pause()` 持久化 `engine_paused=true`。本质问题：**把"需要跨多次调用的状态（确认）/跨多个出价者的级联（代理）"等同于"热引擎做不了 → 整场暂停"**。

但 Lua 决策脚本"只能表达一次 O(1) 决策"是 Lua 的限制，**不是热路径的限制**。证据：同一个引擎**已经能做"非 O(1)"的事**——防狙击延时就是一次决策里同时改价、改赢家、改 `end_at`、改 `extend_count`（`engine.go:309-321`）。既然延时能在热路径原子完成，代理/确认同样可以，只是要换个"摆放位置"。

---

## 2. 反证：行业标杆把这两者放在一起跑（proof by existence）

**CrackingWalnuts《System Design: Online Auction》**（50K bids/sec、1M watcher、p99<200ms）——架构与本项目几乎 1:1（Valkey/Redis CAS 决定谁赢 + Kafka 按 auction 分区 + Postgres fencing token 结算 + 反狙击 + 精确一次），而它的 Feature 清单**同时**包含：English/Dutch/Sealed-bid、**Proxy(auto) bidding**、**Anti-sniping**、**Effectively-once**。它对代理的实现一句话点破：

> Proxy bid cascade：一笔出价被接受后，`proxy-bid-resolver` 消费者读取该拍品的 `proxy_bids`，若他人的隐藏最高价高于新价，就**以其名义、经同一条 API 路径**提交 `current_price + min_increment` 的下一口；级联在"仅剩一个最高价高于当前价"时终止。
> **"从出价处理器的视角，每一笔出价行形状都一样，origin mode 只决定是谁发起的。"** manual / proxy / dutch_accept 走**同一个 Kafka topic、同一个 CAS 脚本、同一组 Postgres 写**。

**eBay 代理出价**（Stanford/Roth 论文、Gixen）：你提交隐藏最高价；系统**按最小加价梯度替你自动出价**，价格上抬到"超过次高最高价"为止（或被他人超过你的最高价）。赢家以"次高最高价 + 一个增量"成交——这是一个**有界、确定、必然终止**的解析（类二价/Vickrey），**绝非无限级联**，因此可以原子完成。

**LMAX 架构**（Martin Fowler）：单线程内存业务处理器 600 万 TPS，**Event Sourcing**——状态完全由输入事件流重放得到，输入先落 journal（durable）再处理。其经典多步流程：下单→发出"待校验"事件→**订单留在内存中等待"已校验"事件**→再确认。**这正是 fat-finger 二次确认的同构形态**（在内存里挂起一个"待确认"状态，等确认事件到达再落定），单写者引擎天然支持。

**交易所撮合 / Akka Cluster Sharding / Orleans / pyemma 拍卖系统设计**：共识是"**对某个实体（一只股票 / 一场拍卖）的所有请求都路由到唯一的有状态单写者**"，状态在内存、按 key 分区、用 WAL+snapshot 或重放恢复。单写者内可跑**任意**业务逻辑且仍是 sub-ms。

> 一句话：**真实工业界用"单写者 + 内存状态 + 持久输入日志"来同时拿到快与富**；代理/确认要么作为"经同一条快路径回灌的另一种出价"，要么作为"单写者状态机里的一段普通逻辑"。**没有人为此把拍卖整场暂停。**

---

## 3. 三种可行架构（按改造成本从小到大）

### 方案 A（最小改动，最贴合现有代码）：富功能 = 喂给同一条快路径的"出价来源"
- **代理/最高出价**：新增 `proxy-bid-resolver` worker。它**消费本项目已有的 Kafka 决策流**（每笔 ACCEPTED 决策已在流里），读取该拍品 ACTIVE 的 `max_bid_intents`，若有人隐藏最高价 > 新当前价，就**以其名义经同一个 `PlaceBid`/同一个 Lua CAS** 注入 `current+increment` 的自动出价；级联有界、按"仅剩一个最高价在上方"终止。秘密最高价存 Redis sorted set（+ PG 持久）。**删掉 `active_max_bid_intent → pause` 判定。**
- **fat-finger 二次确认**：本项目 PG lane **已有** `/api/auctions/{id}/bids/confirm`（`router.go:150` ConfirmBid）。把"待确认"状态从 PG 搬到 Redis：首单若跳价≥阈值，Lua 返回 `CONFIRM_REQUIRED + token`，把 `pending_confirm:{auction}:{user}:{amount}` 写入 Redis（短 TTL）；确认请求带 token 走第二个 Lua 入口落定。**删掉 `fat_finger_threshold IS NOT NULL → pause` 判定。**
- 收益：**几乎不动架构**，复用现有 Kafka 流/Lua/PG/对账；让旗舰引擎直接支持 Demo 主拍品。代价：resolver 注入的自动出价要计入幂等与 seq（沿用现有 `engine_seq`/请求哈希即可）。

### 方案 B（中期，最干净）：把"决策单元"从 Lua 脚本升级为"每拍品单写者状态机"
- 每场拍卖 = 一个**单写者 owner**（Go goroutine/actor 或"按 auction_id 分区、每分区单消费者"），在内存持有拍品状态，串行处理该拍品事件流，**输入日志仍是现有 Kafka WAL**（已具备），Redis 退为读/广播投影，PG 仍为结算真相。
- owner 内可原子跑**任意**逻辑：确认挂起、代理级联、Dutch 降价、保留价、反狙击……都不再受"单条 Lua EVAL"表达力限制，且仍 sub-ms（内存单写者）。
- 这是 **LMAX/Actor 的标准形态**，且对本项目是**演进而非重写**：你已经有 Kafka 有序日志 + 单线程决策思想 + PG 投影 + 对账/重放。补的是"把 Lua 换成可表达多步逻辑的内存状态机 + 每拍品单写者归属（分区租约你也已经有：outbox 16 分片 + relay 租约）"。

### 方案 C（规模化）：按 auction_id 分区的有状态消费集群
- Kafka 分区键 = auction_id；每分区唯一消费者持有该分区内所有拍品的内存撮合状态；passivation（空闲卸载）+ 重放恢复。即交易所"按 symbol 分区撮合"的拍卖版，天然水平扩展，顺带解决"实时扇出仅单机"（中危 M5）。

---

## 4. 为什么团队的直觉"对了一半"

团队**正确**地拒绝把代理级联/确认硬塞进单条原子 Lua EVAL——那确实会让脚本变脏、级联难以表达。但**错误**地由此得出"所以整场退回 PG 并暂停"。行业的正确推论是：

> **"所以把复杂逻辑拆成'经同一条快路径回灌的普通出价'(方案A) 或 '单写者状态机里的一段逻辑'(方案B)，而不是离开快路径。"**

原子决策保持 O(1) 纯净，富功能作为**反馈环 / 状态机逻辑**叠加——这正是 eBay/CrackingWalnuts/LMAX 的共同选择。

---

## 5. 给本仓库的落地清单（最小可演示）
1. 删除 `loadSnapshot` 里 `fat_finger_confirm` / `active_max_bid_intent → RequiresPostgres → pause` 的判定（`engine.go:1079-1116`）。
2. fat-finger：Redis `pending_confirm` 键 + 第二个确认 Lua 入口；H5 既有 `confirm_required` UI 直接复用。
3. proxy：`proxy-bid-resolver` worker 消费现有 Kafka 决策流，按 `max_bid_intents` 注入自动出价经同一 `PlaceBid`；秘密最高价 Redis ZSet + PG。
4. 验证：用本评审用过的"+¥50 交替出价 + 设隐藏最高价"脚本，确认旗舰模式下 **fat-finger 确认通过、代理自动顶价、封顶成交、不暂停**。
5. 文档：把"1000 路 < 50ms"的性能宣称与"支持 fat-finger/proxy"显式合并为同一档位证据（消除 §H1）。

---

## 5.1 2026-06-07 落地复核记录

> 复核时间：2026-06-07 15:02 CST。结论：方案 A 已进入代码实现与快速验证阶段，不再只是架构建议；仍需继续补完整压力证据和更广泛故障矩阵。

### 已在代码中闭合

- `backend/internal/redisengine/engine.go` 的 `loadSnapshot` / `loadSnapshotForRedisState` 已不再把 fat-finger 或 active max-bid 标记为 `RequiresPostgres`，避免旗舰 redis_ledger 因富规则整场 pause。
- Redis 热路径已存在 `pending_confirm` key 与 `confirm_bid_redis_ledger` Lua 入口，`Engine.ConfirmBid` 负责校验 token、过期、金额、当前价重校验和幂等。
- 热路径 max-bid 已经通过同一 `PlaceBid` / CAS 结果面返回最终自动顶价状态，自动出价写入 bids，source 为 `auto_max_bid`，并推进 `engine_seq`。
- Gateway 的 confirm bid 路由仍保留原契约，H5 既有高额确认 UI 可复用，不引入“前端假确认”。

### 已跑验证

- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go test ./internal/redisengine -run 'TestRedisLedger(FatFinger|HotProxy)'`：通过。
  - 覆盖 `TestRedisLedgerFatFinger...`：确认 token 生成、确认后落定、当前价变动后低价确认被拒绝。
  - 覆盖 `TestRedisLedgerHotProxyAutoOutbidsThroughSameCAS`：人工出价触发隐藏最高价自动顶价，响应/Redis 状态/PG 结算均为代理用户领先，`engine_seq=2`，bid source 为 `auto_max_bid`。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。H5/PC 呈现层没有再把热路径能力包装成 debug 黑话。
- Playwright 真实页面点击验证：
  - H5 首屏与半屏显示 `出一手 ¥450.00`、`最新状态已确认`、`已连接`。
  - PC 演示助手抽屉显示 `录屏加速用 · 走真实后端接口`，明确不会前端假改状态。

### 仍需下一轮证明

- 需要补齐更宽的 redis_ledger 回归矩阵：proxy 级联边界、intent exhausted/cancelled、confirm token 重放/错用户/错金额/过期、cap 价与 proxy 同时触发、settlement worker 中断后的恢复。
- 需要把 “1000 路 < 50ms + fat-finger/proxy 同引擎” 合并到正式性能证据前，先跑带富规则的压力脚本，避免把 plain auction 的性能证据外推到富规则。
- PC monitor/host 扩展接口在当前 mock role 下仍有 403 控制台噪声；这不影响热路径结论，但不应在正式演示中被误读为引擎异常。

---

## 5.2 2026-06-07 资金边界回归矩阵补强

> 复核时间：2026-06-07 15:18 CST。结论：redis_ledger 的 fat-finger / hot proxy 已补到资金滥用边界级别；剩余主要是带富规则压力证据与更长故障/恢复链路。

### 新增后端证明

- `TestRedisLedgerFatFingerConfirmRejectsTokenAbuseWithoutDecision`
  - 证明：错用户确认被 `IDEMPOTENCY_KEY_REUSED_WITH_DIFFERENT_REQUEST` 拒绝；错 token 被 `CONFIRM_USED` 拒绝；两者都不推进 `engine_seq`；随后原用户用正确 token 仍可确认；确认请求重放稳定返回同一 `engine_seq`，不重复落账。
- `TestRedisLedgerFatFingerConfirmExpiredTokenDoesNotConsumeValidReplay`
  - 证明：过期 token 被拒绝且不推进热态；原始 PlaceBid 幂等重放仍返回原 confirm-required 结果，不静默接受、不生成资金决策。
- `TestRedisLedgerHotProxyIgnoresCancelledIntent`
  - 证明：已取消的 max-bid intent 不参与自动顶价；人工出价保持人工用户领先；取消状态和 `last_applied_seq=nil` 保持。
- `TestRedisLedgerHotProxyExhaustsIntentBelowNextBid`
  - 证明：隐藏最高价低于下一口时不会注入无效自动出价，intent 标记为 `EXHAUSTED`，人工出价正常结算。
- `TestRedisLedgerHotProxyCanReachCapAndSell`
  - 证明：proxy 可通过同一路径顶到封顶价并直接 `ENGINE_SOLD`；Redis/PG 最终 `engine_seq=2`、赢家为 proxy 用户、生成唯一订单。

### 已跑验证

- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go test ./internal/redisengine -run 'TestRedisLedger(FatFingerConfirm|HotProxy)' -count=1`：通过。
- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go test ./internal/redisengine -count=1`：通过，耗时 136.168s。

### 剩余下一轮

- 带富规则压力证据仍缺：需要构造或扩展 PTS/k6 workload，把 fat-finger/proxy 场景纳入 redis_ledger 性能与正确性证据，不能继续用 plain auction 外推。
- settlement worker 中断后的富规则恢复链路仍需单独故障注入：当前新增矩阵证明了正常 relay/settlement，不等于证明 Kafka/worker crash 后的恢复。
- PC monitor/host 403 噪声与订单长列表仍是演示体验债，不影响本节热路径正确性结论，但仍需进入 UI 下一轮。

## 5.3 2026-06-07 UI audit 剩余体验债回补

> 复核时间：2026-06-07 15:55 CST。结论：本轮没有继续扩展 bid engine 压测；按 `docs/reviews/ui-audit-2026-06-07` 主线处理 PC 演示体验债，避免把热路径证据工作和 UI audit 工作混在一起。

### 已在 UI 中闭合

- PC/H5 demo session 现在会校验目标角色：PC 需要 host 时，如当前 cookie 是 H5 买家 session，会重新登录 host，避免 `/api/monitor/*` 与 `/api/host/*` 因会话污染返回 403。
- PC 订单列表默认折叠为最近 8 条，并提供 `展开全部 N 条 / 收起历史订单`；历史数据仍保留，避免演示首屏被 300+ 订单淹没。
- 订单保证金状态已中文化，不再把 `HELD/FORFEITED/RELEASED` 直接给评委看。

### 已跑验证

- `pnpm build`：通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- Playwright 真实 PC 页面：console error = 0；`/api/monitor/*`、`/api/host/auctions/auc_live/*`、`/api/orders` 均为 200；订单区默认显示最近 8 条且可展开/收起。

### 仍需另开性能/故障链路证明

- 本节不改变 5.2 的性能结论：带 fat-finger/proxy 的正式压力证据、settlement worker 中断恢复证明仍未完成，不能用本地 UI 验证替代。

## 5.4 2026-06-07 UI audit Playwright 门禁补强

> 复核时间：2026-06-07 16:13 CST。结论：本轮仍属于 UI audit，不扩展 bid engine 证据；目标是让 Playwright 测试不再保护旧 debug 呈现，并补上订单详情层级的枚举外泄。

### 已闭合

- PC 订单详情抽屉默认显示人读单号，原始订单 ID 仅作为“排查编号”出现；保证金状态在详情和列表统一中文化。
- PC live e2e 改为真实页面操作当前 UI audit 口径：中文珠宝拍品、按元输入、主控页中文状态、真实创建/排期/开拍/诊断/取消。
- PC mock e2e 改为当前展示台口径：聚合健康灯、中文规则步骤、Arco Upload/DatePicker 真实交互、订单详情中文化断言。

### 已跑验证

- `npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line`：通过。
- `npx playwright test tests/e2e/pc-console.spec.ts --project pc-console --reporter=line`：11/11 通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `pnpm build`：通过。

### 边界说明

- 本轮时诊断 Tab 仍保留运维原始字段，不作为主视频展示面；16:30 CST 后续轮次已做诊断可读化/过滤，但仍不替代 5.2 提到的富规则压力与故障恢复证明。
- `p0smokeseed` 在本轮时尚未承担“清理所有历史 UI live e2e 残留”的职责；16:22 CST 后续轮次已把录制库干净纳入 seed 门禁。

## 5.5 2026-06-07 UI audit 演示数据治理补强

> 复核时间：2026-06-07 16:22 CST。结论：本轮仍只处理 UI audit 的演示数据治理；没有把 seed 清理等同于性能/故障恢复证明。

### 已闭合

- `p0smokeseed` 从只重置 `auc_live/auc_side` 扩展为清理 `room_main/room_side` 下历史演示拍品、竞拍、订单、事件、outbox、Redis engine settlement、snapshot rebuild、AI job、风险告警、系统消息、liveops 和控制信号。
- PC live e2e 不再自己预清理 UI 残留，而是断言 seed 后副直播间没有 `冰种翡翠戒面` 等历史测试拍品；这把“录制库干净”变成可重复门禁。

### 已跑验证

- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend run ./cmd/p0smokeseed`：通过。
- Host API：`room_main` 仅 `auc_live`，`room_side` 仅 `auc_side`。
- `npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line`：通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `pnpm build`：通过。

### 边界说明

- 诊断 Tab 的全局运维表在本轮后仍可能展示压测房间/拍品原始字段；16:30 CST 后续轮次已继续做诊断页可读化/过滤。

## 5.6 2026-06-07 UI audit 诊断页可读化补强

> 复核时间：2026-06-07 16:30 CST。结论：本轮仍属于 `docs/reviews/ui-audit-2026-06-07` 主线，只处理 PC 诊断页展示层；不把它写成 bid engine 性能/正确性证据。

### 已闭合

- PC 诊断表字段中文化：`source/current_price_cents/snapshot_from_db` 等原始 key 改为 `排查入口/当前价/数据库快照` 等可读标题。
- 诊断表值可读化：金额按 `¥` 元展示，时间本地化，延迟按 `毫秒/秒/分钟` 展示，`ACTIVE/CANCELLED/PUBLISHED/BID_TOO_LOW` 等常见状态/拒绝原因翻译为中文。
- 诊断表默认隐藏 `room_engine_*`、`auc_engine_*`、`Engine Item` 和历史测试房间/拍品记录，并明确提示隐藏数量；过滤只发生在前端展示层，后端原始记录仍保留用于排查。
- 竞拍状态表首列改为 `事件回放`，保留点击打开 flight recorder 的追踪能力，不把 `auc_<uuid>` 当首屏业务信息展示。

### 已跑验证

- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend run ./cmd/p0smokeseed`：通过。
- `npx playwright test tests/e2e/pc-console.spec.ts --project pc-console --reporter=line`：11/11 通过。
- `npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line`：通过。
- Playwright MCP 真实页面点击 `http://127.0.0.1:5277/` → `诊断`：竞拍状态表只展示主/副直播间，表头为中文，页面提示 `已隐藏 48 条压测/历史诊断记录`。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `pnpm build`：通过。

### 边界说明

- 这轮不删除后端压测/历史诊断数据，不改变出价判定、结算、Redis/Kafka/PG 证据链。
- 诊断页现在可以短暂作为“运行监控/事件回放”证据入口展示，但主视频仍不应扫全量 Tab；性能与故障恢复仍按 5.2 的独立证据要求推进。

## 5.7 2026-06-07 UI audit reduced-motion 补强

> 复核时间：2026-06-07 16:34 CST。结论：本轮仍是 UI audit 设计系统验收项，补齐 PC reduced-motion 兜底；不改变快引擎方案或性能结论。

### 已闭合

- H5 已有 `prefers-reduced-motion` 规则；PC 本轮新增同类全局兜底，在用户系统偏好减少动效时禁用 animation/transition 并关闭 smooth scroll。
- PC Playwright mock 增加 `reducedMotion: reduce` 用例，真实打开页面并确认健康灯、诊断按钮仍可用，浏览器媒体查询命中。

### 已跑验证

- `npx playwright test tests/e2e/pc-console.spec.ts --project pc-console --reporter=line`：12/12 通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `pnpm build`：通过。
- `npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line`：第一次未先 seed 时失败并暴露历史 `冰种翡翠戒面 ...` 残留；重新执行 `p0smokeseed` 后通过，证明 seed 门禁仍有效。

### 边界说明

- 录制/验收前仍必须先跑 `p0smokeseed`；本轮没有让测试静默清理历史数据。
- reduced-motion 是设计系统体验兜底，不是热路径性能、故障恢复或富规则压力证据。

---

## 参考来源（联网核实）
- CrackingWalnuts《System Design: Online Auction（50K bids/sec, Effectively-Once, Anti-Sniping）》— Valkey CAS + Kafka 分区 + PG fencing + **proxy cascade 经同一路径** + 反狙击 + 精确一次同存。
- Martin Fowler《The LMAX Architecture》— 单线程内存业务处理器 + Event Sourcing + 多步"挂起等待事件"流程。
- A. Roth et al.（Stanford）《The Timing of Bids in Internet Auctions》/ Gixen — eBay 代理出价：隐藏最高价、按增量自动顶价、类二价有界解析。
- Akka Cluster Sharding / Microsoft Orleans / pyemma《How to Design Auction System》— 每实体单写者、内存热态、WAL+snapshot/重放恢复、按 auction_id 分区。
- 交易所撮合引擎（按 symbol 分区、内存单线程撮合、持久日志恢复）。

---

## 6. 方案选型决策（A/B/C 五维对比，作为拓展性思考）

> 结论：**A/B/C 不是必须逐级攀登的阶梯，而是三个不同层面的选择。** 只有 A 直接闭合 H1（被扣分项）；B/C 解决的是单机 Demo 看不到、课题也未要求的"引擎通用性 / 水平扩展"。竞赛阶段选 A（必要时先做 Option 0 兜底），B/C 推迟到"做成真实产品"再说。

| 维度 | **Option 0** 文档分档 + Demo 拍品改 plain | **A** 同路径（proxy resolver + Redis 化确认） | **B** 每拍品单写者状态机 | **C** 分区有状态集群 |
|---|---|---|---|---|
| 效果(评分/H1) | 中：消除"旗舰一出价即翻车"+误导宣称，但未达功能对等 | **高：完全闭合 H1**，可正当宣称"快+富同引擎" | 竞赛低增量(demo 无差别)；产品高 | 竞赛≈0(单机不可体现) |
| 难度 | 极低 | 中（确认易；proxy 中） | 高（换决策基座） | 很高（分布式归属/再平衡） |
| 风险(对既有正确性) | 极低 | **低-中且可控**（核心 CAS 不变；proxy 级联/幂等需护栏） | **高**（改写"已证正确性"皇冠资产，可能回归已通过套件） | 很高，且多数失败模式本机不可复现 |
| 工作量 | ~10 分钟 | 中（数天：小改 + ~12–15 针对性测试） | 大（数周：重写 + 重证全部不变量 + 生命周期测试） | 很大 |
| 不确定性 | 极低 | 低（模式有文档佐证、零件已具备） | 高（归属交接/恢复等价性待证） | 很高（大量本机不可验 → 再造"未验证宣称"） |

**为什么不必一个个来**：A 独立于 B/C，且 A 就足以闭合 H1。B 仅在"Lua + resolver 开始嫌挤、要做长期产品"时才需要；C 仅在"真要多节点水平扩展 + 跨节点扇出"时才需要（课题明确多 Redis/Kafka 缺失不扣分 → 竞赛边际价值≈0）。

**"只要实现就行吗？"——不行**：本项目评分护城河正是"可证明的正确性"。资金/热路径改动若无测试，等于自降质量标准（评委会查 tests/不变量），且 proxy 级联不终止 / 注入出价造成 seq 缺口 / 确认 token 重放等 subtle bug 一旦现场翻车，比"诚实暂停"更糟。但 A 的测试范围是**有界的、小的**（~12–15 个，沿用现有 `redisengine` 集成测试风格）。

**"B/C 要不要补很多测试？"——要，且很多，这本身就是不选它们的理由**：B 强制在新基座上**重证全部旧不变量**（并发封顶仅一 SOLD、幂等 replay/conflict、终态唯一、recovery/rebuild、seq 无缝、fail-closed）+ 新增状态机生命周期测试（passivation/rehydrate、单写者归属交接、多步中途崩溃）。C 还引入大量分布式测试（分区归属、再平衡、脑裂、跨节点扇出），其中很多**单机根本跑不出来 → 永远停在"未验证"**，反把"宣称不可复现"问题再造一遍。"本机测试没关系"救不了你——问题不在跑测试的成本，在**待重证面的体量**与**本机不可验证的面**。
