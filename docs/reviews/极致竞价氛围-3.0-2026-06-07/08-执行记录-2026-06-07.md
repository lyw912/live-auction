# 08 · 执行记录（2026-06-07）

## Round 1 · P0 氛围主舞台闭环

严格按 `00`–`07` 的 P0 路线，本轮只落地能在 H5 主舞台被评委直接看到、且由真实信号驱动的闭环：

- **常驻竞速榜**：`LiveStage` 直接渲染 `RaceBoard`，使用已有 `leaderboard_delta` / leaderboard payload；静默态显示榜一、我当前名次、真实差额，事件态展开 Top 行与我行。
- **出价瀑布基础版**：`leaderboard_delta.seq` 单调递增时生成 `WaterfallChip`，每片绑定真实榜首金额/脱敏用户/当前用户标记；`prefers-reduced-motion` 下隐藏瀑布但保留榜单文本。
- **三态 cue 升级入口**：leaderboard delta 从我领先变为被反超时，主舞台 cue 显示真实差额（如 `差 ¥50.00 · 立即反超`），不再依赖 REST 轮询新鲜度。
- **AI 气氛官分层**：`SYSTEM_AI` 从普通系统飘屏/聊天 overlay 分流为 `CohostRibbon`，明确显示 `AI` 标识；`SYSTEM_TEMPLATE/HOST` 仍走系统飘屏。
- **布局修正**：事件态下 `RaceBoard`、`CohostRibbon`、`system-barrage`、`BidWaterfall` 使用舞台级 `data-race-board` 避让，360px 截图无结构性重叠。

## 代码触点

- `frontend/mobile-h5/src/components.tsx`：新增 `RaceBoard`、`BidWaterfall`、`CohostRibbon`，扩展 `LiveStage` props 与舞台数据属性。
- `frontend/mobile-h5/src/main.tsx`：新增 `waterfallChips`、`raceBoardExpandedUntil`；在 `commitLeaderboardDelta` 中事件驱动瀑布、展开榜、领先/被反超 cue。
- `frontend/mobile-h5/src/styles.css`：新增常驻榜、AI 气氛官、瀑布 canvas 与展开态避让样式；补 reduced-motion 降级。
- `tests/e2e/mobile-h5.spec.ts`：新增 Playwright 用例，模拟真实 WS `leaderboard_delta`，断言主舞台常驻榜、瀑布 canvas、真实差额 cue。
- `docs/reviews/极致竞价氛围-3.0-2026-06-07/capture-atmosphere.mjs`：可复跑视觉取证脚本，注入可控真实事件形状并输出 390/360 截图。

## 验证

- `pnpm --filter mobile-h5 build`：通过。
- `npx playwright test tests/e2e/mobile-h5.spec.ts -g "always-on race board|leaderboard delta" --reporter=line`：3 passed。
- `node docs/reviews/极致竞价氛围-3.0-2026-06-07/capture-atmosphere.mjs`：通过，生成：
  - `evidence/08-race-board-waterfall-ai-390.png`
  - `evidence/09-race-board-waterfall-ai-360.png`
- MCP Playwright 打开真实运行页 `http://127.0.0.1:5276/`：真实后端当前已成交态可见 `RaceBoard` 与 `CohostRibbon`；控制台仅见 `/api/auth/me` 初始 401 登录探测与 `favicon.ico` 404。

## 对照评估

| 文档项 | 本轮状态 | 说明 |
|---|---|---|
| `02` 常驻竞速榜 | **部分完成，主舞台 P0 已可见** | 已外移到 `LiveStage`，WS delta 驱动，事件态展开；后端 delta 契约沿用既有实现。 |
| `03` 出价瀑布 | **基础版完成** | 已有 canvas/rAF/粒子上限/reduced-motion；仍缺完整强度模型与多片 burst 视觉密度调优。 |
| `03` 三态峰值 | **被超越/领先入口完成** | 真实差额 cue 已接 leaderboard delta；落槌高潮未在本轮实现。 |
| `04` AI 气氛官 | **买家侧呈现完成** | `SYSTEM_AI` 明确 AI 标识并从普通飘屏分层；真 LLM API 激活/流式不在本轮改动。 |
| `06` 工程规格 | **按事件驱动落地** | 常驻榜/瀑布不订阅 100ms tick；未改出价热路径。 |

## 下一轮缺口

- 补完整 `IntensityModel` 与 `Gating`，把 `accepted_bids_30s`、price velocity、末段剩余秒数统一映射到 0..3。
- 末段窒息：`going once/二/三` 视觉升温、soft-close 延时大幅可视化、音频心跳床。
- 落槌高潮：礼花/聚光/战绩卡先于支付的完整编排。
- AI 激活：真 `API_KEY` 路径、模板降级路径、事实一致性抽查证据。

## Round 2 · IntensityModel / Gating 底座

本轮补 `01` 与 `06` 明确要求的 L1 纯函数底座，不改出价热路径：

- `frontend/mobile-h5/src/atmosphere.ts` 新增 `calculateAtmosphereIntensity(signals) -> 0|1|2|3`：
  - `accepted_bids_30s`: 1–3 => I1，4–9 => I2，>=10 => I3。
  - `price_velocity_cents_per_min`: >0 => I1，>=5000 => I2，>=12000 => I3。
  - `remainingMS`: <=10s => I1，<=5s => I2，<=3s => I3。
  - `extended` 在已有热度上提升一档，上限 I3。
- `frontend/mobile-h5/src/atmosphere.ts` 新增 `shouldGateAtmosphere(input)`：
  - `recovering/stale/disconnected` 为硬门控，禁止 motion 与 AI。
  - `reducedMotion/lowPower` 禁止 motion。
  - `aiOff` 只关闭 AI，不误伤真实榜单/瀑布 motion。
- `frontend/mobile-h5/src/main.tsx` 将真实 heat + countdown phase 接入强度模型，并将 `data-atmosphere-intensity` / `data-atmosphere-gated` 暴露到 `LiveStage`。
- `frontend/mobile-h5/src/components.tsx` 让 `BidWaterfall` 根据 intensity 调整同屏粒子上限；gated 时不渲染瀑布芯片。
- `frontend/mobile-h5/src/styles.css` 让 I2/I3 影响竞速榜边框/阴影；gated 时对主氛围组件去饱和。

### 验证

- `npx playwright test tests/e2e/atmosphere-engine.spec.ts --reporter=line`：6 passed。
- `pnpm --filter mobile-h5 build`：通过。
- `npx playwright test tests/e2e/mobile-h5.spec.ts -g "always-on race board|leaderboard delta" --reporter=line`：3 passed，包含 `data-atmosphere-intensity="3"` 与 `data-atmosphere-gated="false"` 主舞台断言。
- `node docs/reviews/极致竞价氛围-3.0-2026-06-07/capture-atmosphere.mjs`：通过，复写 390/360 视觉证据。

### 对照评估

| 文档项 | 本轮状态 | 说明 |
|---|---|---|
| `01` L1 IntensityModel | **完成基础版** | 覆盖真实出价速率、涨速、末段秒数、延时提升。 |
| `01` L1 Gating | **完成基础版** | 恢复/过期/断连硬门控，reduced-motion/low-power/ai-off 语义拆分。 |
| `03` 瀑布密度 | **部分接入** | 先按 intensity 调整同屏粒子上限；后续还需 burst 密度和轨迹调优。 |
| `06` 可测试纯函数 | **已覆盖** | `atmosphere-engine.spec.ts` 单测覆盖 intensity/gating 关键边界。 |

### 下一轮缺口

- 末段窒息：把 `data-atmosphere-intensity` 与 `countdownPhase` 变成明确的 going once/二/三 舞台升温视觉。
- soft-close：延时事件的大幅可视化，而不是仅 toast/文案。
- 落槌高潮：礼花/聚光/战绩卡先于支付的完整编排。
- AI 激活：真 `API_KEY` 路径、模板降级路径、事实一致性抽查证据。

## Round 3 · 末段窒息与延时反转层

本轮处理 `03` §C 的主舞台呈现缺口：原先只有 `hammer-beat-layer` 小文字节拍，末段紧张感不够可见。

- `frontend/mobile-h5/src/components.tsx` 新增 `FinalSecondsLayer`：
  - `critical` / 末 5 秒：显示 `最后 5 秒`、剩余秒数、`going once · 盯紧下一口`。
  - `hammer`：显示 `第一次 / 第二次 / 最后一次`，并保留“有效出价仍会延时 / 落槌前最后确认”语义。
  - `extended` cue：显示 `延时 +10s` 与“最后窗口有真实出价，竞拍继续”。
  - `scenario.stale` / `scenario.sold` 下不渲染，避免恢复期或终态播假升温。
- `frontend/mobile-h5/src/styles.css` 将末段层升级为居中峰值层，使用红/青两套态势区分末段与延时反转；不遮挡底部价格、热度、出价 CTA。
- `tests/e2e/mobile-h5.spec.ts` 扩展：
  - 延时事件断言 `final-seconds-layer` 出现 `延时 +10s` 与真实窗口文案。
  - 末 5 秒快照断言主舞台出现 `最后 5 秒`，并保留 authoritative countdown / extension explanation。
- `capture-atmosphere.mjs` 新增 `10-final-seconds-layer-360.png` 视觉取证。

### 验证

- `pnpm --filter mobile-h5 build`：通过。
- `npx playwright test tests/e2e/mobile-h5.spec.ts -g "extension and sold visual|countdown shows stable" --reporter=line`：2 passed。
- `node docs/reviews/极致竞价氛围-3.0-2026-06-07/capture-atmosphere.mjs`：通过，新增：
  - `evidence/10-final-seconds-layer-360.png`

### 对照评估

| 文档项 | 本轮状态 | 说明 |
|---|---|---|
| `03` 末 5 秒 going once | **完成基础版** | 主舞台可见“最后 5 秒”与 going once 文案。 |
| `03` hammer 三拍 | **完成基础版** | 复用服务端校时的 `countdownPhase.beat`，不靠本地假倒计时。 |
| `03` soft-close 可视化 | **部分完成** | `extended` cue 变成主舞台 `延时 +10s` 峰值层；后续可把真实延时秒数从事件中提取显示。 |
| `01` Gating | **保持有效** | stale/sold 不渲染末段峰值层。 |

### 下一轮缺口

- soft-close 层显示真实 `+Ns`，而不是当前固定 `+10s` 文案。
- 落槌高潮：礼花/聚光/战绩卡先于支付的完整编排。
- AI 激活：真 `API_KEY` 路径、模板降级路径、事实一致性抽查证据。

## Round 4 · 落槌高光卡与终态文案修正

本轮处理 `03` §D 的 winner 结果页入口，不把它包装成完整落槌高潮：

- `frontend/mobile-h5/src/result.tsx` 在 winner `ResultSheet` 中新增 `result-climax-card`：
  - 展示 `落槌高光`、成交价、真实有效出价人数与真实出价次数。
  - 明确提示“战绩卡已生成，先确认成交事实再进入支付”，把支付前的高光/事实确认从普通账单中分离出来。
- `frontend/mobile-h5/src/components.tsx` 修正 compact 结果页实际链路：
  - `AuctionStatePanel` 不再把 `heat` 固定传 `undefined`，成交面板能继续读取同一份 `HeatSnapshot`。
  - 成交后 `rank-strip` 与 `bid-hint` 不再显示“下一口/最低有效出价”，改为“已中拍 · 订单待支付 / 成交价 · 订单以服务端为准”，避免终态诱导继续出价。
- `frontend/mobile-h5/src/styles.css` 新增高光卡样式，并修正 compact 结果按钮栅格，390px 截图中结果区按钮高度正常、无挤压。
- `tests/e2e/mobile-h5.spec.ts` 扩展 winner 结果页 Playwright：
  - 断言高光卡包含成交价、`2 人有效出价`、`3 次真实出价`。
  - 断言成交终态不再出现“下一口/最低有效出价”类误导文案。
- `capture-atmosphere.mjs` 新增 `11-winner-climax-card-390.png`：
  - 先通过 WS `leaderboard_delta` 注入真实榜单/热度事实并等待页面显示。
  - 再真实点击 state-matrix 的“成交”按钮进入结果态。
  - 截图前断言高光卡包含非 0 的真实竞拍数字。

### 验证

- `pnpm --filter mobile-h5 build`：通过。
- `npx playwright test tests/e2e/mobile-h5.spec.ts -g "winner result sheet" --reporter=line`：1 passed。
- `node docs/reviews/极致竞价氛围-3.0-2026-06-07/capture-atmosphere.mjs`：通过，新增/复写：
  - `evidence/11-winner-climax-card-390.png`
- 人工目检 `11-winner-climax-card-390.png`：
  - 高光卡显示 `2 人有效出价 · 3 次真实出价`，不是 0 值兜底。
  - 成交后顶部行动条为订单确认语义，不再诱导下一口。
  - 结果卡、分享按钮、高光卡、支付按钮在 390px 截图中无结构性重叠。

### 对照评估

| 文档项 | 本轮状态 | 说明 |
|---|---|---|
| `03` 落槌高潮 | **基础版完成** | 结果页已有支付前高光卡与真实事实展示；尚未实现 2.5s 全屏礼花/落锤音/赢家聚光序列。 |
| `03` 战绩卡先于支付 | **入口完成** | 既有高光卡/短视频按钮保留，高光事实在支付 CTA 之前出现。 |
| `03` 真实事实可追问 | **部分完成** | 竞拍人数/次数来自 `HeatSnapshot`/leaderboard；价格曲线、反超次数、engine_seq 回链仍未实现。 |
| 终态产品安全 | **修正完成** | winner 终态不再显示“下一口/最低有效出价”误导文案。 |

### 下一轮缺口

- 真正的 `ClimaxLayer`：canvas-confetti `useWorker`、落锤音、聚光、约 2.5s 先于支付的完整编排。
- 战绩卡事实深化：价格曲线、反超次数、击败人数与 `engine_seq`/订单可回链。
- reduced-motion 下的落槌高光降级验收。
- AI 激活：真 `API_KEY` 路径、模板降级路径、事实一致性抽查证据。

## Round 5 · soft-close 真实延时秒数

本轮补 `03` §C.2 的诚实性缺口：原先主舞台峰值层固定显示 `延时 +10s`，无法经受不同规则/不同延时时长追问。

- `frontend/mobile-h5/src/domain.ts` 新增 `extendSecondsFromEvent(detail, oldEndAt, nextEndAt)`：
  - 优先使用事件显式 `extend_ms` / `extended_ms`。
  - 没有显式字段时按 `old_end_at` 或前一结束时间与新 `end_at` 计算真实秒数。
- `frontend/mobile-h5/src/main.tsx` 在 `wasExtended` 分支把真实 `+Ns` 写入 `extended` atmosphere cue。
- `frontend/mobile-h5/src/components.tsx` 的 `FinalSecondsLayer` 不再硬编码 `+10s`，而是从 cue detail 提取 `延时 +Ns`。
- `tests/e2e/mobile-h5.spec.ts` 将延时用例从固定 `+10s` 改为事件差值 `+20s`。
- `capture-atmosphere.mjs` 新增 `12-soft-close-plus20-360.png`：在主舞台直接触发连续延时事件，截图前断言 `final-seconds-layer` 显示 `延时 +20s`。

### 验证

- `npx playwright test tests/e2e/mobile-h5.spec.ts -g "extension and sold visual" --reporter=line`：1 passed。
- `pnpm --filter mobile-h5 build`：通过。
- `node docs/reviews/极致竞价氛围-3.0-2026-06-07/capture-atmosphere.mjs`：通过，新增/复写：
  - `evidence/12-soft-close-plus20-360.png`
- 人工目检 `12-soft-close-plus20-360.png`：
  - 主舞台峰值层显示 `延时 +20s`。
  - 底部拍品卡、价格、倒计时、出价 CTA 仍可读。
  - 证据为连续事件 seq，不触发 gap 恢复路径。

### 对照评估

| 文档项 | 本轮状态 | 说明 |
|---|---|---|
| `03` soft-close 可视化 | **真实秒数完成** | 已从固定 `+10s` 改为事件差值/显式毫秒字段驱动的 `+Ns`。 |
| `01` 真实信号门控 | **保持有效** | 仍只在正常 active/非 stale/sold 态显示峰值层。 |
| `05` 禁止假倒计时 | **修正完成** | 延时时长不再由前端硬编码，来自权威事件时间事实。 |

### 下一轮缺口

- soft-close 的倒计时拉伸/竞速榜抖动还只是基础版，需要更大幅但不遮挡的反转动画。
- AI 气氛官还未在真实延时事件上生成真 LLM 话术。
- 真正的 `ClimaxLayer`：canvas-confetti `useWorker`、落锤音、聚光、约 2.5s 先于支付的完整编排。

## Round 6 · 舞台级 ClimaxLayer 与 reduced-motion 验收

本轮补 `03` §D 的主舞台落槌高潮缺口：Round 4 只有结果页高光卡，不能冒充“落槌高潮”。

- `frontend/mobile-h5/src/components.tsx` 新增 `ClimaxLayer` 并挂在 `LiveStage`：
  - 仅在 `scenario.sold && atmosphereCue.kind === 'sold'` 时显示，绑定真实 `auction_sold` cue，不在普通成交结果页常驻假播。
  - 显示 `落槌高光 / 中拍 / 成交价 / 有效出价人数 / 真实出价次数`，事实来自 `HeatSnapshot` 与终态成交价；无 heat 时显示“真实竞拍记录已锁定”，不编造人数。
  - 使用本地 canvas/rAF 生成一次性礼花粒子，聚光层压暗背景，主卡先于支付 CTA 可见。
- `frontend/mobile-h5/src/main.tsx`：
  - sold cue 保留时间从 1.8s 延长到 2.7s，覆盖约 2.5s 高潮窗口。
  - `shouldGateAtmosphere` 接入真实 `prefers-reduced-motion`，不再固定 `false`。
- `frontend/mobile-h5/src/styles.css`：
  - 新增 `climax-layer`、`climax-confetti-canvas`、`climax-spotlight`、`climax-card` 样式与 keyframes。
  - `prefers-reduced-motion` 下隐藏 canvas 礼花并关闭聚光/卡片动画，保留静态事实卡。
  - sold 态不再显示顶部 `atmosphere-cue`，避免遮挡竞速榜和高潮卡；成交事件仍保留 DOM 属性供测试确认。
- `tests/e2e/mobile-h5.spec.ts` 扩展：
  - 正常 sold 事件断言 `climax-layer`、canvas、成交价、`2 人有效出价`、`3 次真实出价`。
  - reduced-motion sold 事件断言 `data-atmosphere-gated="true"`、`data-motion="off"`、无 canvas、静态事实卡仍完整可读。
- `capture-atmosphere.mjs` 新增 `13-climax-layer-390.png`：
  - 用 Playwright 打开真实 H5 页面。
  - 先注入真实 leaderboard/heat 事实，再派发非 gap 的 `auction_sold` 事件。
  - 截取 `live-stage` 本身，避免用结果页半屏卡冒充舞台高潮。

### 验证

- `pnpm --filter mobile-h5 build`：通过。
- `npx playwright test tests/e2e/mobile-h5.spec.ts -g "extension and sold visual|sold climax" --reporter=line`：2 passed。
- `node docs/reviews/极致竞价氛围-3.0-2026-06-07/capture-atmosphere.mjs`：通过，新增/复写：
  - `evidence/13-climax-layer-390.png`
- 人工目检 `13-climax-layer-390.png`：
  - 主舞台可见礼花、成交圆标、聚光压暗、中央 `中拍！¥600.00` 高潮卡。
  - 竞速榜、热度、底部结果 CTA 仍能辨认，无结构性重叠。
  - 高潮卡显示 `2 人有效出价 · 3 次真实出价`，不是 0 值或假热度。

### 对照评估

| 文档项 | 本轮状态 | 说明 |
|---|---|---|
| `03` 落槌高潮 | **主舞台基础版完成** | sold 事件后出现约 2.7s 舞台级礼花/聚光/事实卡，先于支付 CTA 可见。 |
| `03` 礼花瀑布 | **完成本地 canvas 版** | 未引入 `canvas-confetti useWorker`，因当前依赖未安装且不为下载依赖静默降级；本地 canvas/rAF 有粒子上限与 reduced-motion 降级。 |
| `03` reduced-motion | **验收完成** | reduced-motion 下无礼花 canvas/动画，但成交价、真实人数、真实次数和支付前事实仍可读。 |
| `01` Gating | **修正完成** | `main.tsx` 已接真实 `matchMedia('(prefers-reduced-motion: reduce)')`。 |

### 下一轮缺口

- 落锤音仍沿用既有 cue tone/音效包路径，未新增真实采样落锤音与音频解锁后的专属高潮验收。
- 战绩卡事实仍缺价格曲线、反超次数、击败人数和 `engine_seq`/订单回链。
- AI 气氛官还未在真实 `auction_sold` / `extended` 事件上生成真 LLM 话术并做事实一致性抽查。
- soft-close 倒计时拉伸/竞速榜抖动仍是基础版，尚未形成更强的反转动画编排。
