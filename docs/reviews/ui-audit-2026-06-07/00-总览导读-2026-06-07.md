# 00 · 总览与导读：直播竞拍前端审查·重构·演示方案文档群

> 日期：2026-06-07　作者视角：以 10 年+ 字节/TikTok 资深前端 / 产品 / 架构 / 测试 / 评委主席 多视角交叉拷打。
> 取证方式：**真实运行 + 命令行 Playwright 真实点击截图 + 后端/DB 取证 + Tavily/Fetch 联网行业调研（未用 WebSearch）**。结论基于证据，非臆测。
> 边界：本文档群是**审查与方案**，未改动任何业务逻辑代码；仅新增评审文档、截图、与只读截图脚本。期间执行过 `p0smokeseed`（重置演示数据，属“启动/种子”），并在后台启动了 H5/PC 开发服务器用于取证。

---

## 一句话结论

**你不是没做，而是把一套国赛上游的工程，用 debug 半成品的样子端给了评委。** 系统能力很全且能端到端跑通（已实测出价/领先/被超越/落槌成交/复盘）；但默认跑起来满屏英文占位名、原始 UUID、内部黑话、并因演示数据陈旧而 fail-closed 成“看起来坏了”。**和冠军的差距主要在“可读性 + 可信度 + 一支讲清楚的 3 分钟视频”，不在重写系统。** 这是高投入产出比的好消息。

- 今天默认态评委体验：**约 62/100**
- 你实际做到的（证据应得）：**约 84/100**
- 只改呈现层 + 演示数据治理后可达：**88–92/100（夺冠区间）**

---

## 文档群与阅读顺序

1. **`审查与评分报告-2026-06-07.md`** —— 先读。诚实评分、官方功能逐条核对、5 位评委交叉拷打、三大根因、“堆砌清单（该砍/该折叠/该留）”、按优先级的修复清单、冠军差距。
2. **`Demo编排与录制脚本-2026-06-07.md`** —— 你最急需的。一个可信业务故事如何串起所有亮点、“只有一个用户怎么演/会不会太假”的解法、**录前数据治理步骤**、**逐秒 3 分钟分镜**、可直接提交的《核心功能清单》《端到端流程》、防“作假”应答。
3. **`前端重构与设计系统-2026-06-07.md`** —— 怎么改好看且好用。可粘贴的设计 token（暗色 C 端 / 亮色 B 端 / 等宽数字 / 动效 / 声效）、**黑话→人话文案词典**、合规话术、H5 与 PC 的信息架构重构 + ASCII 线框 + 组件“前/后”对照、不重写系统的落地路线与验收清单。
4. **`附录-行业调研汇编-2026-06-07.md`** —— 事实底座。Whatnot/苏富比/佳士得/抖店/得物/微拍堂等标杆的可抄用规范与合规红线，全部附 URL。
5. **`实现工艺手册-工单与验收-2026-06-07.md`** —— **实现阶段必读（给执行模型）**。把方案落成可执行工单：交战规则(防跑偏)、文案/Token 唯一真相源、按 `文件:行` 的 14 条工单（改前→改后 + Arco 配方 + 完成定义 + 验证命令）、验收矩阵、执行协议、Arco/交互配方库。配套机器门禁 `accept.mjs`（当前 ✅16/❌19，目标全绿）。

**证据与脚本**（同目录）：`shots/`（H5/PC 真实截图，覆盖“坏→健康→完整成交仪式”）、`cap.mjs~cap4.mjs`（只读截图脚本，可复跑）、`accept.mjs`（**UI 验收门禁**：禁止/必需字符串扫描 + 关键可交互检查，退出码即“做完了没”）、`shots/structure*.json`（DOM 结构取证）。

> 实现阶段的“怎么干不跑偏、怎样算 100% 做完”全在第 5 篇《实现工艺手册》：它把每个问题钉到 `文件:行`，用 `accept.mjs` 的红→绿作为客观完成判据。

---

## 三大根因（题眼，所有问题归这三条）

- **A 演示数据脏 → 安全降级被读成“坏了”**（P0 阻断）：`auc_live` 已过期、`item_title=NULL`；整库 1628 房间/40 暂停引擎。`p0smokeseed` 重置后 H5 立即健康可出价。
- **B 表达层 debug 级**：英文占位名、`engine_<uuid>`/`ord_<uuid>`、`待同步 50·最久 1859758ms`、`seconds_since_last_bid:9999`、价格按“分”输入、原生控件。
- **C 黑话 + 内部状态机外泄 + IA 堆砌**：`本地到零/权威价格同步中`、领先时仍显示“暂不提交出价”、主屏唯一 CTA 是“看拍品信息”而非出价、演示驱动与真实控制混排。

> 修复 = **翻译（黑话→人话）+ 分区（技术指标/演示工具下沉）+ 给主路径（出价）+ 治理演示数据**。基本不碰业务逻辑。

---

## 立刻要做的（按顺序）

1. **录前数据治理**：`env $(grep -vE '^\s*#|^\s*$' .env | xargs -d '\n') go -C backend run ./cmd/p0smokeseed`，确认 H5 显示“当前最高价/剩余时间/可出价”。（详见《Demo》§1）
2. **换中文珠宝拍品**（改种子一行或现场创建），别让 `P0 Live Smoke Item` + 陶艺图出镜。
3. 按《审查报告》§7 的 **P0+P1**（多为 S/M 工作量）清掉表达层噪音、修领先态文案、价格按元、演示助手分区。
4. 按《Demo》逐秒脚本录 3 分钟（左 H5 / 右 PC），附架构图与 PTS-1B 性能证据作附录。

---

## 不要做的（避免稀释）

- 不要再加新功能（福袋/PK/答疑只会让评委问“这在干嘛”）。
- 不要在主视频里扫诊断 Tab、开三个以上窗口、或把演示驱动当普通功能展示。
- 不要用“拍卖/开始拍卖/拍卖成功”——抖音内是**竞拍**；拍品要标“支持 7 天无理由”。

---

## 运行环境备注（取证时状态）
- 基础设施（postgres/redis/kafka/minio）healthy；后端 `:18080` readyz ok。
- 已在后台启动 `pnpm dev:h5`(:5276) 与 `pnpm dev:pc`(:5277) 用于截图；如需停止可自行结束对应进程。
- 已执行一次 `p0smokeseed`，`auc_live` 现为健康可出价状态（end_at 为执行时 +10 分钟，过期后再跑一次即可）。
