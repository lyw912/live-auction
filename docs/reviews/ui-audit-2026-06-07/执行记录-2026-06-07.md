# UI 重构执行记录

> 时间：2026-06-07 11:43 CST

## 范围

- 按《实现工艺手册-工单与验收-2026-06-07.md》执行 WO-0 至 WO-14 的呈现层重构。
- 后端仅改 `backend/cmd/p0smokeseed/main.go` 的演示种子数据和演示 Redis 残留清理，不改竞拍判定、出价规则、结算、幂等或 API 契约。
- H5/PC 均用真实 dev server + Playwright 点击验证，不只做代码静态检查。

## 已落地

- H5：中文珠宝拍品、珠宝素材、出价主路径、领先态人话文案、证书可核验/7 天无理由、黑话和原始订单 ID 清理、金额千分位。
- PC：顶栏聚合健康灯、房间下拉只保留运营房间、订单人读单号、金额按元输入、健康标签中文化、演示助手抽屉化、Arco 上传/日期组件。
- 设计 token：补齐 H5 暗色主题、PC 亮色主题、红/蓝/金/成功/危险语义色和 tabular 数字。
- 验收门禁：`accept.mjs` 改为优先点击稳定 `data-testid`，断言未放宽。

## 素材

- 当前项目素材：
  - `frontend/mobile-h5/public/demo/jade-pendant.jpg`
  - `frontend/mobile-h5/public/demo/jade-live-loop.mp4`
- 外网 Wikimedia 下载在当前环境超时，未作为本次最终素材来源。若替换素材，保持上述同名路径即可，无需改代码。

## 验收

- `pnpm build`：通过。
- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend run ./cmd/p0smokeseed`：通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- Playwright 手动验证：
  - H5 主屏渲染中文珠宝拍品和出价按钮。
  - H5 点击出价后进入“你已领先，暂无需加价”。
  - H5 拍品与规则展示“GID 20260607 · 可核验”和“支持 7 天无理由”。
  - PC 顶栏展示“引擎 ● 正常”，房间下拉仅主/副直播间。
  - PC 演示助手抽屉展示“录屏加速用 · 走真实后端接口”。

## 剩余风险

- PC 订单列表仍会展示历史订单很多条；本次按手册只做人读单号与脱敏，未扩展分页/筛选。
- PC 监控接口在当前 mock role 下仍有 403 控制台噪声；UI 已 fail-closed 展示，未在本轮改权限契约。

## 追加复核：2026-06-07 15:02 CST

### 本轮补强

- H5 主路径 CTA 统一为 `出一手 ¥{下一口}`，覆盖首屏吸底卡、竞拍半屏、静态演示 scenario、被拒绝后重试态。
- H5 正常连接态从 `已同步` 改为 `已连接`；结果/订单/恢复反馈中的 `同步` 改为 `确认/生成/更新` 语义。
- H5 热度卡修正 30 秒窗口与累计出价混排：近 30 秒无热度但已有历史出价时显示 `累计 N 次出价 / 最新状态已确认`，不再同时显示“还没有人出价”和“累计 N 次出价”。

### 真实页面验证

- Playwright 打开 H5 `http://127.0.0.1:5276/`：首屏显示 `天然翡翠A货平安扣吊坠`、`累计 2 次出价`、`最新状态已确认`、`出一手 ¥450.00`、`已连接`。
- Playwright 点击 H5 `进入竞拍面板`：半屏显示 `最新状态已确认`、`最低有效出价 ¥450.00`、按钮 `出一手 ¥450.00`。
- Playwright 打开 PC `http://127.0.0.1:5277/`：顶栏显示 `引擎 ● 正常`，房间下拉仅 `主直播间/副直播间`。
- Playwright 点击 PC `打开演示助手`：抽屉显示 `录屏加速用 · 走真实后端接口`，按钮包括 `模拟另一位买家出一手`，说明 `不会前端假改状态；每次都会写入真实竞拍记录、事件和订单`。

### 命令复核

- `rg -n "已同步|同步中|同步订单|同步服务端|同步|出价 ¥" frontend/mobile-h5/src`：0 命中。
- `pnpm build`：通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go test ./internal/redisengine -run 'TestRedisLedger(FatFinger|HotProxy)'`：通过。

### 仍披露

- PC 页面在当前 mock role 下仍有 monitor/host 扩展接口 403 控制台噪声；已确认不是本轮 H5 呈现补丁引入。
- PC 订单列表历史数据过长仍是已知体验债；本轮未扩展分页/筛选。

## 追加复核：2026-06-07 15:18 CST

### 本轮补强

- 新增 redis_ledger 资金边界集成测试，覆盖 fat-finger token 错用户/错 token/过期/重放，以及 hot proxy 的 cancelled intent、below-next-bid exhausted、cap sold。
- 未改 UI 与生产逻辑；本轮主要把《统一快引擎与富功能》里“仍需更宽回归矩阵”的一部分转化为可重复测试。

### 命令复核

- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go test ./internal/redisengine -run 'TestRedisLedger(FatFingerConfirm|HotProxy)' -count=1`：通过。
- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go test ./internal/redisengine -count=1`：通过，耗时 136.168s。

### 仍披露

- 带 fat-finger/proxy 的压力证据仍缺；下一轮应扩展 PTS/k6 或补一个专用富规则 pressure smoke，避免性能宣称继续从 plain auction 外推。
- settlement worker 中断后的富规则恢复链路仍需故障注入证明。
- PC mock role 403 与订单长列表仍未处理。

## 追加复核：2026-06-07 15:55 CST

### 本轮补强

- 回到 `docs/reviews/ui-audit-2026-06-07` 主线，处理上一轮 UI audit 披露的两项体验债：PC host/monitor 接口 403 控制台噪声、订单列表历史数据过长。
- H5/PC `ensureDemoSession(account)` 增加目标角色校验：若当前 cookie 是买家 session，PC 请求 host 时会重新登录 host，避免同域 H5/PC 互相污染会话。
- PC 订单面板默认只展示最近 8 条，提供 `展开全部 N 条 / 收起历史订单`，不删除任何后端订单数据。
- PC 订单保证金状态从 `HELD/FORFEITED/RELEASED` 翻译为 `保证金已冻结/已扣除/已释放`，避免内部枚举外泄。

### 真实页面验证

- Playwright 新开 PC `http://127.0.0.1:5277/`，等待加载后 console error 为 0。
- Playwright 网络面板确认 `/api/monitor/*`、`/api/host/auctions/auc_live/*`、`/api/orders` 均为 200，不再出现 403。
- Playwright 订单区显示 `展开全部 354 条`、默认 8 条订单、`已收起 346 条历史订单；演示时默认只展示最近 8 条。`
- Playwright 点击 `展开全部 354 条` 后按钮变为 `收起历史订单`，再点击可收起。
- 订单行显示人读单号与中文保证金状态，如 `单号 JP20260607-... / 保证金已扣除 / 保证金已冻结`。

### 命令复核

- `pnpm build`：通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `git diff --check`：通过。

### 仍披露

- 当前演示库仍有大量历史订单数据；本轮只做前端折叠与演示态治理，未清库、未改后端分页契约。
- 顶栏显示 `引擎 ● 异常` 时来自真实监控数据，不再是 403/session 污染造成的假异常；正式录制前仍应按 Demo 文档重置/治理演示数据。

## 追加复核：2026-06-07 16:13 CST

### 本轮补强

- 继续严格沿 `docs/reviews/ui-audit-2026-06-07`，不进入 bid engine / load test 主线。
- PC 订单详情抽屉改为默认展示人读单号 `JPYYYYMMDD-...`，原始 `ord_*` 仅作为 `排查编号` 出现，避免把技术主键当业务单号给评委看。
- PC 订单详情里的保证金状态复用列表同一中文映射：`保证金已冻结/已扣除/已释放`，不再在详情里泄漏 `HELD/FORFEITED/RELEASED`。
- `tests/e2e/pc-console-live.spec.ts` 从旧的 `P0 Live Smoke Item / Side Room Smoke Item / Live PC Item`、按“分”输入、英文状态断言，升级为中文珠宝拍品、按元输入、主控页中文状态、真实 UI 预清理残留后再创建/排期/开拍/诊断/取消。
- `tests/e2e/pc-console.spec.ts` 补订单详情抽屉点击断言，并把旧的 `pending Redis`、`Product/Price/Trust`、原生 file/date 操作改为当前 UI audit 后的中文聚合健康灯、中文规则步骤、Arco Upload/DatePicker 真实交互口径。

### 真实页面验证

- Playwright live PC：`npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line` 通过，真实页面完成副直播间切换、UI 取消上次失败残留、创建中文珠宝拍品、按元保存规则、排期、开拍、诊断表确认、取消清理。
- Playwright mock PC：`npx playwright test tests/e2e/pc-console.spec.ts --project pc-console --reporter=line` 通过，11/11；覆盖订单详情中文化、顶栏聚合健康灯、Arco 上传、Arco 日期、规则按元输入。

### 命令复核

- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `pnpm build`：通过；仍只有既有 Vite chunk-size warning。
- `git diff --check`：通过。
- `rg -n "P0 Live Smoke Item|Side Room Smoke Item|Live PC Item|pending Redis|append 7/1/0|Verified bidder gate|suffix=['\"]分|拍卖|权威价格同步中|本地到零|seconds_since_last_bid" frontend/mobile-h5/src frontend/pc-console/src tests/e2e/pc-console.spec.ts tests/e2e/pc-console-live.spec.ts`：仅剩 `promptMetricCopy` 对 `seconds_since_last_bid` 的翻译分支，页面输出为“最近出价：暂无出价”，不是原始哨兵上屏。

### 仍披露

- 诊断 Tab 在 16:30 CST 追加轮次前仍是运维排查表，会显示原始 `ACTIVE/CANCELLED`、压测 `Engine Item`、技术 ID 等后端原始字段；后续轮次已单独处理诊断页过滤/字段翻译。
- `p0smokeseed` 在本轮时尚未清理所有 live e2e 创建的副直播间历史拍品；16:22 CST 轮次已把录制库干净纳入 seed 门禁。

## 追加复核：2026-06-07 16:22 CST

### 本轮补强

- 按 UI audit 的演示数据治理要求扩展 `backend/cmd/p0smokeseed`，仍只改演示种子/清理脚本，不触碰出价判定、结算、幂等或 API 契约。
- seed 现在会清理 `room_main/room_side` 下除固定种子外的历史拍品、竞拍、订单、事件、outbox、Redis engine settlement、snapshot rebuild、AI job、风险告警、系统消息、liveops 和控制信号，避免 PC live e2e 或录制前被旧 `冰种翡翠戒面 ...` 等残留污染。
- `tests/e2e/pc-console-live.spec.ts` 去掉 UI 预清理分支，改为断言 seed 后副直播间不再包含 `冰种翡翠戒面`。这样 seed 若回退，真实 Playwright 会失败，而不是测试自己把脏数据擦掉。

### 真实数据验证

- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend run ./cmd/p0smokeseed`：通过。
- Host API 验证：
  - `/api/rooms/room_main/auctions`：只返回 `auc_live`，拍品 `天然翡翠A货平安扣吊坠`，状态 `ACTIVE`。
  - `/api/rooms/room_side/auctions`：只返回 `auc_side`，拍品 `和田玉福牌吊坠`，状态 `DRAFT`。
- Playwright live PC：`npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line` 通过，且断言副直播间没有历史 `冰种翡翠戒面` 残留。

### 命令复核

- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `pnpm build`：通过；仍只有既有 Vite chunk-size warning。
- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend test ./cmd/p0smokeseed`：通过。
- `git diff --check`：通过。

### 仍披露

- 诊断 Tab 在本轮后仍可能展示历史压测房间/拍品的原始运维表字段；16:30 CST 轮次已继续做诊断表展示层过滤/翻译。

## 追加复核：2026-06-07 16:30 CST

### 本轮补强

- 继续严格沿 `docs/reviews/ui-audit-2026-06-07`，只处理 PC 诊断 Tab 可读化/过滤，不回到 bid engine、压测或结算逻辑主线。
- `MonitorTable` 增加诊断展示适配层：字段标题从 `source/current_price_cents/snapshot_from_db` 等原始 key 改为 `排查入口/当前价/数据库快照` 等中文；金额按 `¥` 元展示，时间按本地时间展示，毫秒延迟按 `毫秒/秒/分钟` 展示。
- 常见状态与拒绝原因翻译为人话：`ACTIVE/CANCELLED/PUBLISHED/BID_TOO_LOW` 等不再作为诊断表主文本外露，分别显示 `开拍中/已取消/已推送/低于当前可出价`。
- 诊断表默认只展示主/副直播间或无房间归属的运维记录；`room_engine_*`、`auc_engine_*`、`Engine Item`、历史测试房间/拍品行在展示层隐藏，并在页面明示 `已隐藏 N 条压测/历史诊断记录，原始数据仍保留在后端用于排查。`
- 竞拍状态表的首列从原始 `auc_<uuid>` 改为 `事件回放` 入口，保留点击打开 flight recorder 的运维追踪能力，不把技术主键当评委主信息。

### 真实页面验证

- Playwright mock PC：`npx playwright test tests/e2e/pc-console.spec.ts --project pc-console --reporter=line` 通过 11/11；断言无效出价显示 `低于当前可出价`，恢复记录显示 `主直播间/数据库快照`，且 `tr_reject` 仍可点击打开事件回放。
- Playwright live PC：`npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line` 通过；真实页面完成副直播间创建中文珠宝拍品、保存规则、排期、开拍、进入诊断、取消清理，并断言竞拍状态行显示 `开拍中 / 副直播间 / 冰种翡翠戒面 ...`，不含 `ACTIVE`、`Engine Item`、`auc_engine`。
- Playwright MCP 真实页面点击 `http://127.0.0.1:5277/` → `诊断`：竞拍状态表显示 `排查入口 更新序号 状态 直播间 拍品 当前价 结束时间 有效出价 延时次数 最近事件`，仅主/副直播间两行，并显示 `已隐藏 48 条压测/历史诊断记录`。

### 命令复核

- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend run ./cmd/p0smokeseed`：通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `pnpm build`：通过；仍只有既有 Vite chunk-size warning。
- `git diff --check`：通过。

### 仍披露

- 诊断 Tab 现在可作为“运行监控/事件回放”的证据入口短暂展示，但它仍是运维排查区，不应在主视频里扫全量 Tab。
- 本轮是展示层过滤与文案可读化，不删除后端压测/历史诊断数据，也不替代快引擎性能、故障恢复或富规则压力证据。

## 追加复核：2026-06-07 16:34 CST

### 本轮补强

- 对照《前端重构与设计系统》验收 checklist 的 `prefers-reduced-motion` 项继续收口；H5 已有 reduced-motion 规则，本轮补齐 PC 全局 reduced-motion 兜底。
- PC 样式在 `@media (prefers-reduced-motion: reduce)` 下禁用 animation/transition 并关闭 smooth scroll，避免评委或用户系统偏好减少动效时仍触发不必要运动。
- `tests/e2e/pc-console.spec.ts` 新增真实页面用例：模拟 `reducedMotion: reduce`，打开 PC 页面，确认健康灯与诊断按钮仍可见，并验证浏览器媒体查询命中。

### 真实页面验证

- Playwright mock PC：`npx playwright test tests/e2e/pc-console.spec.ts --project pc-console --reporter=line` 通过 12/12。
- Playwright live PC 第一次未先 seed，真实失败暴露副直播间仍有上一轮已取消的 `冰种翡翠戒面 ...` 残留；这符合 16:22 CST seed 门禁的设计，不静默忽略。
- 重新执行 `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend run ./cmd/p0smokeseed` 后，`npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line` 通过。

### 命令复核

- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `pnpm build`：通过；仍只有既有 Vite chunk-size warning。
- `git diff --check`：通过。

### 仍披露

- 录制/验收前必须先跑 `p0smokeseed`；否则真实 live 页面会忠实展示历史已取消拍品，Playwright 会失败而不是帮忙清理。
- 本轮只补设计系统动效偏好兜底，不改变页面信息架构、出价链路或诊断数据。

## 追加复核：2026-06-07 17:16 CST

### 本轮补强

- 继续严格沿 `docs/reviews/ui-audit-2026-06-07`，只处理 H5/PC 页面呈现与 Playwright 门禁；没有回到 bid engine、压测或结算主线。
- H5 liveops 暖场任务里的 `看拍品`、`看榜单` 现在从半屏内按钮进入对应 sheet 时也会完成真实 liveops 任务，不再只打开页面但漏记任务。
- H5 `stateMatrix=1` 调试矩阵下，恢复快照不再把用户手动选择的恢复/断开/终态场景强制覆盖回 `active_bids`；正常演示入口仍跟随服务端快照。
- `tests/e2e/mobile-h5.spec.ts` 从旧 UI 文案迁移到当前 UI audit 口径：`出一手 ¥...`、`已连接`、`次出价/次`、`拍品与规则/出价榜/自动加价`、人读订单号、无 raw snapshot debug 文案。
- H5 Playwright 对恢复、stale snapshot、sound opt-in、动效、订单实时事件的断言改为真实事件顺序：连续 `seq` 才触发动效/支付状态，跳号或 stale snapshot 走恢复保护且 CTA 禁用。

### 真实页面验证

- MCP Playwright 打开 H5 `http://127.0.0.1:5276/`：首屏显示 `天然翡翠A货平安扣吊坠`、`近30秒 1 人 · 1 次出价`、`已连接`、`出一手 ¥450.00`。
- MCP Playwright 点击 H5 `进入竞拍面板`：竞拍面板显示 `¥400.00`、`剩余 08:55`、`我的排名 #1 · 当前领先`、`最低有效出价 ¥450.00 · 按 ¥50.00 加价`、主按钮 `出一手 ¥450.00`，底部快捷入口为 `拍品与规则/出价榜/自动加价/更多`。
- MCP Playwright 打开 PC `http://127.0.0.1:5277/`：主控页显示 `天然翡翠A货平安扣吊坠`、订单区默认 8 条并提示 `已收起 346 条历史订单`，订单号为 `JP20260607-...`。
- MCP Playwright 点击 PC `诊断`：诊断页进入成功，页面展示中文 `出价确认链路`、`待确认`、`最长延迟`、`竞拍状态/出价确认/无效出价/恢复记录` 等标签。

### 命令复核

- `npx playwright test tests/e2e/mobile-h5.spec.ts --project mobile-h5 --reporter=line`：53/53 通过。
- `npx playwright test tests/e2e/pc-console.spec.ts --project pc-console --reporter=line`：12/12 通过。
- `npx playwright test tests/e2e/visual-regression.spec.ts --project visual-mobile-h5 --reporter=line`：7/7 通过；H5 visual 对价格/倒计时/CTA 动态区域做固定遮罩，避免秒级时间漂移伪失败。
- `npx playwright test tests/e2e/visual-regression.spec.ts --project visual-pc-console --reporter=line`：1/1 通过。
- `pnpm build`：通过；仍只有既有 Vite chunk-size warning。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：第一次未先 seed 时 H5 live 必需项失败，暴露演示库状态不干净；执行 `p0smokeseed` 后复跑为 ✅35 / ❌0 / ⚠️0。
- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend run ./cmd/p0smokeseed`：通过。
- `npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line`：1/1 通过。
- `git diff --check`：通过。

### 仍披露

- `accept.mjs` 和 live PC 仍依赖先执行 `p0smokeseed`；不先 seed 时页面会忠实显示当前真实库状态，门禁应失败而不是静默清理。
- 本轮新增的是 H5/PC 真实页面 Playwright 门禁和两个 H5 呈现/任务状态修复，不改变出价判定、结算、Redis/Kafka/PG 证据链。

## 追加复核：2026-06-07 17:31 CST

### 本轮边界校正

- 用户指出当前应严格参考 `docs/reviews/ui-audit-2026-06-07`，本轮已停止把 bid engine / redisengine 测试当作 UI audit 验收依据。
- 复读《总览导读》和《实现工艺手册》后确认：UI audit 边界是 H5/PC 呈现层、演示数据治理和真实 Playwright 页面验收；后端只允许 `p0smokeseed` 录制数据治理，不以出价引擎测试替代页面验收。

### 本轮补强

- 首次直接跑 `accept.mjs` 时 H5 live 必需项失败 3 个；执行 `p0smokeseed` 后复跑恢复为 ✅35/❌0/⚠️0，证明失败来自当前演示库未先治理，而不是静默放宽门禁。
- H5 mock 全量 Playwright 初跑暴露 2 个 UI 测试失配：快速重复点击用例没有等待 state matrix 的 `竞价中` 态真正接管，延时/成交动效用例在同一强动效生命周期内连续断言两个 cue。
- `tests/e2e/mobile-h5.spec.ts` 改为复用 `selectActiveBidsState(page)`，先确认 `出一手 ¥400.00` 可用，再用真实点击 + `waitForRequest('/api/auctions/auc_live/bids')` 验证本地出价锁只发 1 次请求。
- 成交动效用例等待 `extended` cue 回到 `none` 后，再派发连续 `seq` 的 `auction_sold` 事件，并补齐事件里的 `end_at/server_time_ms`，验证 sold 动效本身而不是和前一个 cue 生命周期竞态。
- 未改 H5/PC 生产代码、后端逻辑、出价判定、结算或 API 契约。

### 真实页面验证

- MCP Playwright 打开 H5 `http://127.0.0.1:5276/`：首屏显示 `天然翡翠A货平安扣吊坠`、`已连接`、`当前最高价 ¥400.00`、`近30秒 1 人 · 1 次出价`、`出一手 ¥450.00`。
- MCP Playwright 点击 H5 `进入竞拍面板`：半屏显示 `¥400.00`、`剩余 08:29`、`我的排名 #1 · 当前领先`、`最低有效出价 ¥450.00 · 按 ¥50.00 加价`、主按钮 `出一手 ¥450.00`，快捷入口为 `拍品与规则/出价榜/自动加价/更多`。
- MCP Playwright 打开 PC `http://127.0.0.1:5277/`：主控页显示 `主直播间/副直播间`、`引擎 ● 异常`、订单默认 `展开全部 354 条`，订单号为 `JP20260607-...`，保证金状态为 `保证金已扣除/保证金已冻结`。
- MCP Playwright 点击 PC `诊断`：页面显示中文 `出价确认链路`、`待确认`、`待入账`、`最长延迟`、`竞拍状态/出价确认/无效出价/恢复记录` 等证据入口。

### 命令复核

- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend run ./cmd/p0smokeseed`：通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：seed 前忠实失败，seed 后 ✅35 / ❌0 / ⚠️0。
- `npx playwright test tests/e2e/mobile-h5.spec.ts --project mobile-h5 --reporter=line -g "local bid lock|extension and sold"`：2/2 通过。
- `npx playwright test tests/e2e/mobile-h5.spec.ts --project mobile-h5 --reporter=line`：53/53 通过。
- `npx playwright test tests/e2e/pc-console.spec.ts --project pc-console --reporter=line`：12/12 通过。

### 仍披露

- UI audit 当前机器门禁与真实页面验收均通过；录制/验收前仍必须先跑 `p0smokeseed`，否则真实库状态会导致 H5 live 门禁失败。
- PC 顶栏的 `引擎 ● 异常` 来自真实监控状态，不是 session/403 污染；作为诊断证据可保留，但主视频仍不应长时间扫诊断页。
- 本轮只闭环 UI audit Playwright 与真实页面验证，不替代 `docs/reviews/统一快引擎与富功能-行业调研与方案-2026-06-07.md` 中另行披露的富规则压力/恢复证据缺口。

## 追加复核：2026-06-07 17:56 CST

### 本轮边界

- 本轮没有改 H5/PC 页面；UI audit 仍维持 seed 后 `accept.mjs` ✅35/❌0/⚠️0、H5 mock 53/53、PC mock 12/12 的闭环状态。
- 按总目标转入 `docs/reviews/统一快引擎与富功能-行业调研与方案-2026-06-07.md` 的剩余证据缺口，补充的是 `redis_ledger` 富规则恢复/pressure smoke 后端集成测试；不把它写作 UI audit 的替代验收。

### 关联验证

- 新增 `TestRedisLedgerHotProxyBatchReplayAfterWorkerCrashIsIdempotent`：证明 hot proxy 连续 accepted batch 在 settlement worker DB commit 后、offset commit 前重放时 exactly-once settle。
- 新增 `TestRedisLedgerRichRulesPressureSmoke`：24 轮 fat-finger/proxy 交替，42 条富规则决策全部 settle，无 pause、无重复 seq、无非终态 settlement。
- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend test ./internal/redisengine -count=1`：通过，140.243s。

### 仍披露

- 这不是正式 UI 页面轮次，也不是正式 1000 VU PTS；UI 录制前仍需 seed + Playwright 真实页面检查。

## 追加复核：2026-06-07 18:13 CST

### 本轮边界校正

- 严格回到 `docs/reviews/ui-audit-2026-06-07` 主线，只处理 H5/PC 呈现层、Playwright 真实页面验收和执行记录。
- 未改后端出价判定、结算、幂等、Redis/Kafka/PG 契约；live 验收前仍按手册先执行 `p0smokeseed`。

### 本轮补强

- H5 结果态不再从 `user_2` 等技术 ID 截取 `us**` 展示赢家；`auction_sold`、snapshot 和出价响应都会优先使用 `leader_user_masked`，输家结果显示如 `赵** 以 ¥600.00 中拍`。
- H5 结果态与高光卡统一合规文案：`恭喜中拍`、`本场已落槌`、`已落槌`、`次出价`；清掉结果态残留的 `拍中/落锤/口有效出价/SCHEDULED` 上屏问题。
- H5 Playwright 事件用例继续按真实顺序校验：终态事件先进入 `active_bids` 连续 seq；准备出价用例等待真实 stepper 金额变化，避免快速点击竞态误报。
- PC 队列/控场的 `0 口/口有效出价` 改为 `0 次出价/次有效出价`，规则校验提示改为 `预计 N 次加价到顶`。
- PC 诊断摘要的 `最近恢复 -ms` 改为 `最近恢复 暂无记录` 或 `最近恢复 {人话耗时}`，避免裸哨兵式指标上屏。

### 真实页面验证

- MCP Playwright 打开 H5 `http://127.0.0.1:5276/`：首屏显示 `天然翡翠A货平安扣吊坠`、`已连接`、`剩余 09:18`、`出一手 ¥450.00`。
- MCP Playwright 点击 H5 `进入竞拍面板`：半屏显示 `¥400.00`、`我的排名 #1 · 当前领先`、`最低有效出价 ¥450.00 · 按 ¥50.00 加价`、主按钮 `出一手 ¥450.00`；点击主按钮后未出现 `暂不提交出价` 旧文案。
- MCP Playwright 打开 PC `http://127.0.0.1:5277/`：主控页正常显示中文拍品、订单折叠与人读单号；控制台仅 favicon 404。
- MCP Playwright 点击 PC `诊断`：页面显示中文 `出价确认链路`、`待确认`、`待入账`、`最长延迟`、`最近恢复 暂无记录/人话耗时` 等诊断入口。

### 命令复核

- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend run ./cmd/p0smokeseed`：通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `npx playwright test tests/e2e/mobile-h5.spec.ts --project mobile-h5 --reporter=line`：53/53 通过。
- `npx playwright test tests/e2e/pc-console.spec.ts --project pc-console --reporter=line`：12/12 通过。
- `npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line`：seed 后 1/1 通过。
- `pnpm build`：通过；仍只有既有 Vite chunk-size warning。
- `git diff --check`：通过。

### 仍披露

- `pc-console-live` 会创建临时副直播间拍品；跑完 live 后如果要录制或跑 `accept.mjs`，必须重新执行 `p0smokeseed`。
- H5/PC 页面仍会出现 favicon 404 控制台噪声；不影响页面流程，本轮未把 favicon 纳入 UI audit 工单范围。

## 追加复核：2026-06-07 19:27 CST

### 本轮补强

- H5 `ResultSheet` 从 `components.tsx` 拆到 `result.tsx`，保留支付、复制复盘、高光卡和短视频生成能力；主组件瘦身但不改变成交/支付/结果语义。
- H5/PC mock 数据统一为中文珠宝拍品：`天然翡翠A货平安扣吊坠`、`和田玉福牌吊坠`，可视回归快照同步更新。
- H5 弹幕展示不再把 `user_2` 截成 `us**`，统一显示 `匿名买家`；PC `maskUser` 对 `user_*` 也显示 `匿名买家`，已有中文脱敏名如 `张**` 保持不变。
- H5 live Playwright 测试修正为用户可读断言：页面断言 `竞价中/已连接/专场 side/中文拍品/匿名买家`，API 仍断言 `auc_live/auc_side/status` 等真实后端契约。测试不再要求 `ACTIVE`、`room_side` 这类内部枚举/技术 ID 上屏。

### 真实页面验证

- MCP Playwright 打开 H5 `http://127.0.0.1:5276/rooms/room_main`：首屏显示 `竞拍专场`、`已连接`、`竞拍中`、`GID 20260607 · 可核验`、`天然翡翠A货平安扣吊坠`、`累计 1 次出价`、`出一手 ¥400.00`，弹幕展示 `匿名买家`。
- MCP Playwright 点击 H5 `进入竞拍面板`：半屏显示 `竞价中`、`¥350.00`、`剩余 09:26`、`已连接`、`最低有效出价 ¥400.00 · 按 ¥50.00 加价`、主按钮 `出一手 ¥400.00`。
- MCP Playwright 点击 H5 主按钮后进入领先态：显示 `你已领先`、`我的排名 #1 · 当前领先`、`你已领先，暂无需加价`，主按钮为禁用的 `已领先`；未出现 `暂不提交出价`。
- MCP Playwright 打开 PC `http://127.0.0.1:5277/`：主控页显示 `主直播间`、聚合灯 `引擎 ● 异常`、中文拍品、`2 次有效出价`、订单人读单号 `JP20260607-...`、订单折叠提示；发现并修复领先者/中标人 `us**` 展示问题。
- MCP Playwright 控制台仅见初始 `/api/auth/me` 401 探测和 `favicon.ico` 404 噪声；未见渲染异常。

### 命令复核

- 串行执行 `p0smokeseed -> mobile-h5-live -> p0smokeseed -> pc-console-live -> p0smokeseed -> accept`，避免 mutating live 用例互相污染真实库。
- `LIVE_AUCTION_H5_URL=http://127.0.0.1:5276 npx playwright test --config tests/e2e/playwright-live.config.ts --project mobile-h5-live --reporter=line`：2/2 通过。
- `npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line`：1/1 通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `npx playwright test tests/e2e/mobile-h5.spec.ts --project mobile-h5 --reporter=line`：53/53 通过。
- `npx playwright test tests/e2e/pc-console.spec.ts --project pc-console --reporter=line`：12/12 通过。
- `pnpm build`：通过；仍只有既有 Vite chunk-size warning。

### 仍披露

- `mobile-h5-live` 会真实把 `auc_live` 推到封顶成交并支付；`pc-console-live` 会真实创建/取消副直播间拍品。任何 live 验收、录制或 `accept.mjs` 前都必须重新执行 `p0smokeseed`，且这些 mutating live 用例不能并行跑。
- PC 顶栏 `引擎 ● 异常` 来自真实监控状态，不是 UI 文案回退；诊断页仍是解释该状态的证据入口。
- favicon 404 和初始 `/api/auth/me` 401 是当前开发服务噪声，本轮未纳入 UI audit 工单范围。

## 追加复核：2026-06-07 19:46 CST

### 本轮补强

- 复核 `前端重构与设计系统` §7 时发现 PC 竞拍主控右栏仍把诊断枚举带到主路径：`Recovery pressure`、`RATE_LIMIT_REDIS_DOWN`、`REDIS_ENGINE_ACCEPTED_PUBLIC_SEQ_GAP` 以及待推送/无效出价计数。已改为中文业务摘要：`恢复链路有压力`、`限流服务异常`、`推送序号待恢复`，并把诊断计数下沉为 `监控入口：诊断页查看`。
- `EventTimeline` 仍保留 `事件回放` 入口，但最近事件卡片不再直接渲染后端枚举；主控页只展示 `异常/竞拍事件/出价/推送`、`事件 #seq` 或短排查号。
- `riskQueue`/`connectionLabel` 改成人话输出，避免在 PC ①②③屏继续出现恢复链路英文术语；诊断页仍保留工程排查能力。

### 真实页面验证

- MCP Playwright 点击 PC `诊断`：真实页面显示 `出价确认链路`、`待确认 0`、`待入账 2/7`、`最长延迟 120ms`、`竞拍状态/出价确认/无效出价/恢复记录`；竞拍状态表隐藏 49 条压测/历史记录，未把 `ACTIVE`、`Engine Item`、`auc_engine` 作为运营主路径文本展示。
- MCP Playwright 点击 PC `竞拍`：修复前右栏可见 `Recovery pressure`、`RATE_LIMIT_REDIS_DOWN`、`REDIS_ENGINE_ACCEPTED_PUBLIC_SEQ_GAP`；修复热更新后同页改为 `恢复链路有压力`、`限流服务异常`、`推送序号待恢复`、`事件 #42`。
- MCP Playwright 点击 `打开演示助手`，再点击 `模拟另一位买家出一手`：PC 当前价从 `¥400.00` 更新到 `¥450.00`，证明“第二买家出一手”不是同价假反超。
- MCP Playwright 新开 H5 `http://127.0.0.1:5276/rooms/room_main`：买家端同步显示 `当前最高价 ¥450.00`、下一口 `出一手 ¥500.00`，飘屏 `价格来到¥450.00，榜首已刷新。`，证明 PC 演示助手通过真实后端链路推到买家端。

### 命令复核

- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend run ./cmd/p0smokeseed`：通过。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0；在 PC live 之后重新 seed 又跑一次，仍 ✅35 / ❌0 / ⚠️0。
- `npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line`：1/1 通过。
- `pnpm build`：通过；仍只有既有 Vite chunk-size warning。
- `git diff --check`：通过。

### 仍披露

- 设计系统 §7 的阶段三明确是“可选打磨”：全量巨石拆分、珠宝循环视频、声效 opt-in、动效曲线并未全部完成；本轮只确认 WO-0..WO-14、阶段一、阶段二和评分报告 P0/P1/P2 呈现问题。
- `pc-console-live` 和手动演示助手点击都会改变真实竞拍数据；最终验收、录制和 `accept.mjs` 前仍必须重新执行 `p0smokeseed`。

## 追加复核：2026-06-07 20:22 CST

### 本轮补强

- 新增 `设计系统落地验收矩阵-2026-06-07.md`，把《前端重构与设计系统》§1/§2/§3/§4/§5/§6/§7 拆成可观察页面状态，并明确 `已落地/已验证/部分完成/未完成`，避免把视觉与 IA 要求混同于 `accept.mjs`。
- 设计 token 前提补齐到 `frontend/shared-design/tokens.css`，覆盖暗色 C 端、亮色 B 端、CTA/危险/成功/警告/金色、边框、半屏、卡片、chip 等变量；H5/PC root 改为引用共享 token，减少两端漂移。
- 按 §5 安装 `@icon-park/react` 到 H5；H5 一线动作栏、证书、珠宝、物流、声音启用 IconPark 多彩图标。B 端继续使用 Arco + Lucide。
- H5 首屏商品卡增强为可观察的吸底出价主路径：`当前 ¥350.00 -> 出一手 ¥400.00`，并保留点击进入完整竞拍半屏。
- PC IA 改为设计稿四工作区：`开播中控 / 拍品与规则 / 订单记录 / 运行监控`；订单从开播中控移入独立 `订单记录` 工作区。
- PC 订单改为表格：`单号 / 买家 / 金额 / 状态 / 支付截止 / 保证金 / 操作`，金额右对齐，买家脱敏，技术 ID 只进详情和事件回放。

### 真实页面验证

- MCP Playwright H5 `http://127.0.0.1:5276/rooms/room_main`：首屏显示 `竞拍专场`、`GID 20260607 · 可核验`、`顺丰包邮`、`天然翡翠A货平安扣吊坠`、`当前 ¥350.00 -> 出一手 ¥400.00`；右侧动作栏为商品/互动/点赞/更多 IconPark 图标。
- MCP Playwright PC `http://127.0.0.1:5277/`：侧栏显示四工作区 `开播中控 / 拍品与规则 / 订单记录 / 运行监控`；开播中控只保留队列、当前拍品控制和直播助手。
- MCP Playwright 点击 `订单记录`：页面显示订单表，列为 `单号 / 买家 / 金额 / 状态 / 支付截止 / 保证金 / 操作`，行内展示 `JP20260607-...`、`买家 匿名买家`、`¥150.00`、`详情/审计`。

### 命令复核

- `pnpm --store-dir /tmp/pnpm-store --filter mobile-h5 add @icon-park/react`：通过，新增 H5 IconPark 依赖。
- `pnpm build`：通过；仍只有既有 Vite chunk-size warning。
- `npx playwright test tests/e2e/mobile-h5.spec.ts --project mobile-h5 --reporter=line`：53/53 通过。
- `npx playwright test tests/e2e/pc-console.spec.ts --project pc-console --reporter=line`：12/12 通过。
- `npx playwright test --config tests/e2e/playwright-live.config.ts --project pc-console-live --reporter=line`：1/1 通过。
- `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOPATH=/tmp/go-path go -C backend run ./cmd/p0smokeseed`：通过；普通沙箱曾因本地 DB socket 权限失败，已按权限流程提升后执行。
- `node docs/reviews/ui-audit-2026-06-07/accept.mjs`：✅35 / ❌0 / ⚠️0。
- `git diff --check`：通过。

### 仍披露

- H5 设计稿里的“滑动出一手”交互尚未实现为 drag 阈值确认；当前是首屏吸底出价主路径 + 半屏点击 CTA + 高额确认。
- PC `拍品与规则` 已有价格/时间/信任/预览分组，但设计稿四组发布表单里的履约字段仍没有完整独立字段化。
- 阶段三全量巨石拆分、完整动效曲线逐项验收仍未完成；本轮只完成设计前提、核心 IA、素材/组件库和关键页面可观察状态。
