# P0 Load Smoke Baseline

Date: 2026-05-22

Commit: pending

Script:
- `pnpm test:load:p0`
- `backend/cmd/p0loadsmoke`
- `backend/internal/realtime` reconnect-storm test

## Environment

- CPU: local Windows host, 20 logical CPUs reported by Go
- RAM: not captured
- Disk: not captured
- OS/Kernel: Windows, Go runtime `windows/amd64`
- Go: `go1.26.3`
- PostgreSQL: local Docker Compose via `DATABASE_URL`
- Redis: `localhost:6379`
- k6: not installed in this environment
- ulimit -n: not applicable on Windows shell
- somaxconn: not captured
- ephemeral port range: not captured
- CPU governor: not captured
- Docker/native: Go process on Windows, PostgreSQL/Redis local Docker infra

## Dataset

- rooms: one generated hot room plus existing local test data
- auctions: one generated ACTIVE auction for bid/outbox smoke
- users: 65 generated load users
- watchers: 12 in-process hub subscribers
- bidders: 24 concurrent bid attempts
- seed command: built into `go run ./cmd/p0loadsmoke`

## Workload

- scenario: local P0 load smoke, not a capacity benchmark
- duration: single short run
- warmup: none
- VUs/connections: 24 concurrent bid goroutines; 12 in-process fanout subscribers
- bid pattern: one hot auction, concurrent increasing bid amounts, expected accepted/rejected mix
- reconnect/slow-client pattern: reconnect storm covered by focused realtime test; slow consumer covered by bounded hub queue overflow

## Runs

| Run | Result | Errors | p50 | p95 | p99 | Notes |
|---|---|---:|---:|---:|---:|---|
| 1 | PASS | 0 | 159ms | 267ms | 285ms | local bid burst smoke only |
| 2 | not run | | | | | P0 smoke, not 3-run final baseline |
| 3 | not run | | | | | P0 smoke, not 3-run final baseline |

## Raw Output

```json
{
  "commit_hint": "",
  "generated_at": "2026-05-22T07:54:46.6416613Z",
  "environment": {
    "goos": "windows",
    "goarch": "amd64",
    "go_version": "go1.26.3",
    "cpus": 20,
    "database": "PostgreSQL via DATABASE_URL",
    "redis_addr": "localhost:6379",
    "native_tool": "go run ./cmd/p0loadsmoke"
  },
  "final_second_bid_burst": {
    "result": "PASS",
    "attempts": 24,
    "accepted": 3,
    "rejected": 21,
    "errors": 0,
    "duration_ms": 299,
    "p50_ms": 159,
    "p95_ms": 267,
    "p99_ms": 285,
    "seq_continuous": true,
    "notes": [
      "valid concurrent bids intentionally race on one hot auction row",
      "latency values are smoke measurements, not final baseline claims"
    ]
  },
  "outbox_burst": {
    "result": "PASS",
    "errors": 0,
    "duration_ms": 105,
    "outbox_processed": 27,
    "notes": [
      "processes pending outbox generated by bid burst and setup events",
      "does not claim sustained hot-table throughput"
    ]
  },
  "watcher_fanout_and_slow_consumer": {
    "result": "PASS",
    "watchers": 12,
    "messages": 8,
    "healthy_received": 96,
    "slow_closed": true,
    "duration_ms": 0,
    "errors": 0
  },
  "known_limits": [
    "local smoke baseline only; not a QPS/P99/fanout capacity claim",
    "k6 is not installed in this environment",
    "single process, small workload, local Docker infra"
  ]
}
```

Reconnect storm focused raw output:

```text
go test ./internal/realtime -run TestSnapshotRebuildSingleflightBoundsReconnectStorm -count=1 -v
=== RUN   TestSnapshotRebuildSingleflightBoundsReconnectStorm
--- PASS: TestSnapshotRebuildSingleflightBoundsReconnectStorm (0.09s)
PASS
ok  	live-auction/backend/internal/realtime	0.232s
```

## System Metrics

- CPU: not sampled
- RSS: not sampled
- heap: not sampled
- GC: not sampled
- goroutines: not sampled
- FDs: not sampled

## DB Metrics

- lock wait: not sampled
- tx duration: not sampled
- slow queries: not sampled
- deadlocks: not sampled
- pool wait: not sampled

## Redis Metrics

- command latency: not sampled
- memory: not sampled
- evictions: not sampled
- blocked clients: not sampled

## Bottleneck

No bottleneck claim. This smoke only proves the local scripts can exercise hot-row bid contention, outbox relay drain, in-process fanout, slow-consumer close, and reconnect-storm singleflight without failing their invariants.

## Verdict

Measured claim allowed:
- Local P0 load smoke passed on this Windows/Docker environment.

Known limits:
- This is not a final Linux/native 3-run baseline.
- k6 is not installed; no k6 HTTP/WS capacity benchmark was run.
- No QPS, online user capacity, or production P99/P999 claim is allowed from this file.

Next action:
- Before any performance number appears in README/demo/slides, run proper k6 scripts on a documented native environment and store 3 raw runs.
